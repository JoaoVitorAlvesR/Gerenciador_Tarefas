import { TarefasConcluidaDirective } from './tarefas-concluida.directive';

describe('TarefasConcluidaDirective', () => {
  it('should create an instance', () => {
    const directive = new TarefasConcluidaDirective();
    expect(directive).toBeTruthy();
  });
});
